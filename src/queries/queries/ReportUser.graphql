mutation ReportUser($description: String!, $type: Int!, $badUsername: Boolean!, $targetId: UUID!) {
	reportUser(
		description: $description
		type: $type
		badUsername: $badUsername
		targetId: $targetId
	) {
		ok
	}
}